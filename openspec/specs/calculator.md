# Simple Calculator
- Backend: Python Flask (`app.py`, `requirements.txt`).
- Frontend: HTML/CSS/JS (`templates/index.html`).
- Routes: `/` for UI, `/calculate` (POST) for math.
- Operations: Add, Subtract, Multiply, Divide.